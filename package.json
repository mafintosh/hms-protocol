{
  "name": "hms-protocol",
  "description": "The protocol stream that hms uses to communicate between servers and clients",
  "version": "1.2.2",
  "repository": "git://github.com/mafintosh/hms-protocol.git",
  "dependencies": {
    "cable": "~1.3.0"
  },
  "devDependencies": {
    "standard": "^2.4.1",
    "tap": "~0.4.8"
  },
  "scripts": {
    "test": "standard && tap test.js"
  }
}
